type Props = {
    className?: string;
};

const GamePage = (props: Props) => {
    return (
        <div className={props.className}>
            <div>GamePage</div>
        </div>
    );
};

export default GamePage;
